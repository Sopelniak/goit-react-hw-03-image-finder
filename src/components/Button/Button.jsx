import PropTypes from 'prop-types';
import './Button.scss';

export const Button = () => {
  return (
    <button className="Button" type="button">
      Load more
    </button>
  );
};

Button.propTypes = {
  //   prop: PropTypes,
};
